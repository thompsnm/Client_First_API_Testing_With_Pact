language: node_js
node_js:
  - "4.4.4"
env:
  - TEST_DIR=restaurant-type-consumer
before_install:
  sudo apt-get install ruby-full &&
  gem install bundler
install:
  cd $TEST_DIR &&
  npm run travis_install
script: npm test
